name: Agenda V2 Marca (disabled during CRM stabilization)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow intentionally disabled
        run: echo "Automatic index.html mutation disabled during deep-review stabilization."

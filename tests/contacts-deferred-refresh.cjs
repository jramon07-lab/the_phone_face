const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');
const s=fs.readFileSync('js/modules/contacts-list-ui.js','utf8');
let reads=0,hidden=true;
const node={classList:{contains:()=>hidden,add(){},remove(){}}};
const ctx={state:{rows:[{id:'1'}],selected:new Set(),salesRevision:0,filters:{labels:[]}},byId:()=>node,fetchAllContacts:async()=>{reads++;return [{id:'1',data:{NOMBRE:'Actualizado'}}]},loadGlobalLabels:async()=>{},fetchOpportunities:async()=>[],needsLabels:()=>false,renderSources(){},applyAndRender(){},setStatus(){},window:{dispatchEvent(){}},CustomEvent:function(){},console,Map,Set,Promise};vm.createContext(ctx);
vm.runInContext(s.slice(s.indexOf('let contactsLoad=null'),s.indexOf('async function fetchOpportunities'))+ '\nthis.load=loadContacts;this.invalidate='+s.match(/invalidate:(\(\)=>\{contactsStale=true;.*?return loadContacts\(true\);\})/)[1],ctx);
(async()=>{await ctx.load();assert.equal(reads,0);await ctx.invalidate();assert.equal(reads,0,'saving in chat does not reload hidden contacts');await ctx.load();assert.equal(reads,1,'returning to contacts fetches fresh data');await ctx.load();assert.equal(reads,1,'unchanged list reuses cache');hidden=false;await ctx.invalidate();assert.equal(reads,2,'visible list refreshes immediately');console.log('PASS deferred contacts: hidden, return, cache, visible');})().catch(e=>{console.error(e);process.exit(1)});
